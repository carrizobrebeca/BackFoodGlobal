import { Parameter } from "./parameter";
export interface ILine {
    isValid(): boolean;
    isEmpty(): boolean;
    getLiteral(): string;
    clear(): void;
}
/**
 * Parameterized line
 */
export declare class ParameterizedLine implements ILine {
    prefix: string;
    module: string;
    lineType: string;
    parameters: Parameter[];
    constructor(module: string, lineType: string, parameters?: Parameter[], prefix?: string);
    isValid(): boolean;
    isEmpty(): boolean;
    getLiteral(paramSep?: string): string;
    clear(): void;
    private getParameterLiteral;
}
/**
 * Parameter without parameterization
 * Empty line and comment line should be stored using RawLine.
 */
export declare class RawLine implements ILine {
    content: string;
    constructor(content: string);
    isValid(): boolean;
    isEmpty(): boolean;
    getLiteral(): string;
    clear(): void;
}
